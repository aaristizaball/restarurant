%div.categories
  -0.step(@categories.size-1,3) do |i|
    %div.row-fluid
      -for j in 0..2
        -if i+j <= @categories.size-1
          -category = @categories[i+j]
          =link_to image_tag("/assets/categories/#{category.title}.png"), category_path(category)