%h3=@category.title
%div.dishes
  -@dishes.each do |dish|
    %a=link_to image_tag("/assets/dishes/#{dish.category.title}/#{dish.image}.jpg"), dish_path(dish)
    %br
    %a=link_to dish.title, dish_path(dish)
%div#pagination=paginate @dishes